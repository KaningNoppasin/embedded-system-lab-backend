meta {
  name: CreateUser
  type: http
  seq: 2
}

post {
  url: {{BASE_URL}}:{{PORT}}/users
  body: json
  auth: inherit
}

body:json {
  {
    "rfid": "new-rfid"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
